---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: resourcegroupdeployments.resources.klaudio.nubank.io
spec:
  group: resources.klaudio.nubank.io
  names:
    kind: ResourceGroupDeployment
    listKind: ResourceGroupDeploymentList
    plural: resourcegroupdeployments
    singular: resourcegroupdeployment
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ResourceGroupDeployment is the Schema for the resourcegroupdeployments
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ResourceGroupDeploymentSpec defines the desired state of
              ResourceGroupDeployment
            properties:
              parameters:
                type: object
                x-kubernetes-preserve-unknown-fields: true
              placement:
                type: string
              refs:
                items:
                  properties:
                    apiVersion:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - apiVersion
                  - kind
                  - name
                  type: object
                type: array
              resources:
                items:
                  properties:
                    name:
                      type: string
                    properties:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    resourceRef:
                      type: string
                  required:
                  - name
                  - properties
                  - resourceRef
                  type: object
                type: array
            required:
            - placement
            type: object
          status:
            description: ResourceGroupDeploymentStatus defines the observed state
              of ResourceGroupDeployment
            properties:
              status:
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
